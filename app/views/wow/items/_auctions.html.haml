- if auctions.blank?
  %p
    No auctions.
- else
  %table.table.table-condendsed
    %thead
      %tr
        %th As of
        %th Quantity
        %th Buyout
        %th Owner
        %th Time Left
    %tbody
      - auctions.each do |auction|
        %tr
          %td
            = time_ago_in_words auction.snapshots.last.created_at
            ago
          %td= auction.quantity
          %td= wow_price auction.buyout
          %td= auction.owner_name
          %td= auction.snapshots.last.time_left
